#!/usr/bin/env bash

set -e

BUILD_DIR="build"

echo "Building..."

cmake --build "$BUILD_DIR"

echo "Running tests..."

ctest --test-dir "$BUILD_DIR" --output-on-failure

echo "pre-push checks passed"